# Dependencies
node_modules/

# Build output (www/ is now tracked — APK always has latest content without npm run build)
# www/

# Android build artifacts
android/app/build/
android/.gradle/
android/build/
android/capacitor-cordova-android-plugins/build/

# Capacitor-synced web assets (rebuilt via npm run build && cap sync)
android/app/src/main/assets/public/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
get-youtube-token.ps1

# Android signing — NEVER commit the release keystore or its credentials.
# See android/app/keystore.properties.example for local setup instructions.
android/app/*.keystore
android/app/keystore.properties
